# study
参考サイト
https://qiita.com/ysdyt/items/5972c9520acf6a094d90
https://docs.opencv.org/3.1.0/d3/db4/tutorial_py_watershed.html

# やること
- 日大のデータセットからテストデータとして10データ持ってくる(すでにある)
  - 1データ読み込む (DataLoadTest.ipynb)
  - 読み込んだデータをアノテーションする
  - ラベル画像を保存する (save_lbl_mini.py)
- できたアノテーションを吉川さんに見せて判定

### 仕様
- input 画像
  - 歯のみ抽出された画像
- seed画像
  - ラベリングされた画像
    - 同一の歯は同一のラベリングがされていること
  - seed の準備
    - 1. ImageJ で1データ1歯でのアノテーションをする (黒)
    - 2. 二値化して反転する(背景+アノテーション領域を255にする)
    - 3. 連結成分を抽出する
    - 4. すべての成分を同じ値にする (1データ1歯のため)
### 日大のデータセットについて
- たくさんデータがある
  - みんなで分担してアノテーションを作る

# study
参考サイト
https://qiita.com/ysdyt/items/5972c9520acf6a094d90
https://docs.opencv.org/3.1.0/d3/db4/tutorial_py_watershed.html

勉強順序
1. watershed 法を理解する
   どんなアルゴリズムであるか、どんなアルゴリズムならいい感じになるかを考える
2. Graph-cutなどを使って手動でアノテーションをしてみる
   その時にwatershed 法のアルゴリズムを参考にする?
3. 再度watershed 法に戻って2での内容を参考にアルゴリズムを作る
# memo
コインの個数は24個
# 20250625
watershedについてアルゴリズムを理解した。自作のwatershedアルゴリズムを作ってみる?
-> 作った。サンプルコードの方がはるかに精度が良い。
# 20250627
cv2.watershedのソースコードがある
"D:\_study\OpenCV\modules\imgproc\src\segmentation.cpp"
まずはこれを理解しよう
# 20250704
同じマーカーを用いて自作とopencvのwatershedを適用する
※周囲探索の時に未探索領域のラベリングをしていなかったので修正
※savefigの時にタイムスタンプ機能を付ける
# 20250722
独学のアルゴリズム作成は無理かも
simageを実験してみた。いい感じにできている。-> まだノイズ除去していないので、改善はできそう。
opencvとは異なり輪郭を付与することはできず、オブジェクトの分類ができるだけなので、手動で輪郭を用意したところうまくいってそう。
重なっている部分が別のオブジェクトとして処理されている。
合間の処理とどう元画像に落とし込むかが課題。
西村さんより-> シード値の設定をいじったらいいのではないか
               skimageでのmask?もっと深堀をするとdistanceより距離の大きい点をマスクにしているので、その箇所を変更したらよさそう？
         　-> コインよりも実画像で行った方がよいかも
# 20250807
シード値を出力してみよう -> 手動で修正したらきれいになった
シードを自動で削除するようにしてみたい

距離行列を求めてみたい。距離の近いところは駆除するように？
# 20250821
距離が10未満は削除としたらマーカーが隣接したオブジェクトがあって1つ消えた
領域内の個数で判別することでいらないシードを削除することができた。
第1章完。

# 2026/02/01
再始動。歯の推論自体はできていそうなので、歯の分割を試みる
# 2026/02/19
skimageのやつをもう少し深堀りする
# 2026/02/20
skimageの引数について、以下を工夫する
- markers: シード点 -> 3次元ではどんな色塗りなら許される?
- connectivity: 6
- watershed_line: True
- dtype: uint16
# 2026/02/23
markersを編集した。ちょっとはいい感じ
インプットが二値化画像なので、閾値処理はいらない。
省略した。
配列を回転させて推論する順番を変えてみる?
->  意味なし
# 2026/02/23-2
seedをうまく変更させてみたい。
いったんパワープレイで手塗してみる
  ImageJで色を黒にして1歯1データとして保存する
  読み込みは黒のところで、孤立している場合にseedとする? 二値化で白にするのはうまくいかなかった
    黒の島が5x5以内だったらとかにしてみるか?
# 2026/02/26
## 画像処理研究会発表
咬合のデータではうまくいかないことから、他のデータでやってみてはどうかとアドバイス
以下の要領で進めることに
- いい感じのデータでは、理想通りのものが出力されるのか。
   - 今回の手法と同じで、適当な1スライスでラベリングするようにする
- いい感じのデータが量産できれば、それをもとにセグメンテーションするモデルを作成する
# 2026/03/05
西村さんにアノテーションされたデータをもらうまで待機
保存コードとかはもらったので、使い方を学ぶ?
# 2026/03/12
西村さんの Load プログラムをローカルに分割した。
スライス表示は nodebook でやらないとだけど notebook が使えない問題はある
とりあえず numpy のデータを return するのはできたっぽい
label_map の 3,4 が歯のアノテーション部分
testデータの10ファイルはtif形式で保存できた
できれば番号付けもしてほしいらしいが、正解がわからない
 -> ReLu の推論を使うことでうまくイカナイカ？
# 2026/03/13
- watershedのシード編集を修正した
  - ImageJでseedにしたい歯の色を消す(_annotate.tif)
  - inputで編集前、編集後の画像の差分をとって、それをseedにする
- とりあえずは1歯1アノテーションで、独立しているところでマーキングする
- label_map_1
  - 2歯でつながった
    - ラベリングはできていて、1~10スライスはできているが、11スライス目以降でつながった(アノテーションは10スライス)
    - 上顎でのアノテーションはできているので、歯が上下端点すべてそろっていないと厳しい？左右は見切れていてもいいことは確認済み
# 2026/03/19
- 0-120でトリミングして実験してみた
  - 上顎の歯も見切れている画像
  - 結果は上顎はうまくいっていて、下顎の結果は変わらず
  - 切れていてもよさそう?
- 効果なし
  - seed 伝播 (近傍) の値を変えてみる
  - seed に closing をする
- 効果あり
  - seed に使用するものを変えてみた(annotate_rev2)
    - つながるところの seed に対して、より近づけた場所で seed を作成した。
      - 遠すぎる -> 隣接がどこかわからないとなるので、際々のところでラベルを付けた
- 次やること
  - 距離変換での状態を調べる
    - 初期値と seed の位置関係に相関があるか
      - 距離変換したときに値が15を下回るようなところで seed を作成した(annotate_rev3)
        - 浸食
      - 下顎のアノテーションスライスを18にした(17)
        - 下側の距離は5, 上側は12程度
        - うまくいった22スライスは7程度
        - 結果: 下側のみ浸食
  - 他のでーたでも効果があるか検証
# 2026/03/23
- サンプルデータで Seed の状態を変えて検証
  - 距離変換時に、遠点の領域が含まれていないとうまく分割ができないかも
  - これを歯のアノテーション時にどう応用するか検討する
  - 根管をふさいだ方が距離画像が一様になるので精度が上がる
# 2026/03/26
- 画像処理研究会
  - 距離変換の指標を決める
- 距離画像の二値化してみる
# 2026/04/02
- 座標からラベル探索するようなコードを実装中
  - 座標と探索スライス間を設定すると、隣接領域とつながらない最大の場所でラベリングをしてくれる
  - スライスの自動指定ができれば、点を打つだけで自動化できそう
- 岡西さん的には、そもそもの歯の抽出がうまくできないとだめでしょとか
# 2026/04/03
- WaterShedのアノテーションを半自動でできるように改善する
  - 引数は file_path (二値化画像で根幹は埋めているもの)のみ
  - GUI 上でポイントを作成する
     - マーキングした座標はコンソール上ではなくGUI上に表示させてみたい
  - マーキングした座標を引数にSeedを作成する
    - 指定した座標の Connected component での面積プロファイルを見て、急な増大を検知して判定する
  - 大元の file_path と作成した Seed を引数に WaterShed を実行する
    - Seed は numpy でもよいが、後の確認のため tif 形式での保存、マーキングの座標は残しておくべき?
# 2026/06/04
- 手塗り無しの全自動でできるようにする
  - Watershedはseedした歯の面積が大きかったらうまくいくことが分かった。
- グラフを使って自動化する
  - 以下処理
    1. volumeを3次元配列にする
    2. 各スライスでConnected Componentでラベリング
    3. ラベル画像に対して、ラベルごとの面積も情報として入れる
       -> tuple: (frame, height, width)=(label, sum) 背景はlabel=0
    4. グラフのノードを重複のないラベルとし、重みは(Slice, label, sum)とする
       ※ labelも重みにしているのは、同一スライスで同じ面積が出たときに区別するため
    5. iスライスのラベル成分ごとに画素(i,w,h)=label_iを走査する
       1. i-1スライスの同一画素との積multi=label_i*label_{i-1}が0でない場合
       2. かつすでに出た積でない場合
       3. (i,w,h)=label_iと(h,w,label_i,s1), (i-1,w,h)=label_{i-1}と(h,w,label_{i-1},s2)はそれぞれ対応している。特にlabel_i=multi/label_{i-1}であるので、
       4. ノード(i,label_i, s1), (i-1, label_{i-1}, s2)に辺を引く
       5. 辺を引いたときの積multiを記録する
       6. 次の画素へ
     6. できた重み付きグラフに対して、エッジを切る
        1. 下顎
           1. iスライスのノードGを走査する
              1. Gとエッジの伸びているi-1スライスのノードの個数nを数える
              2. nが2以上の場合
                 1. ノードG_jを走査 (j\in n)
                 2. i-2以下でエッジが伸びているノードを走査する
                    1. ラベル用にマーキングされているノードが存在する場合何もしない
                    2. ラベル用にマーキングされているノードが存在しない場合、ノードG_jにマーキングする(重みを追加する?)
        2. 上顎
           1. iスライスのノードGを走査する
              1. Gとエッジの伸びているi+1スライスのノードの個数nを数える
              2. nが2以上の場合
                 1. ノードG_jを走査 (j\in n)
                 2. i+2以下でエッジが伸びているノードを走査する
                    1. ラベル用にマーキングされているノードが存在する場合何もしない
                    2. ラベル用にマーキングされているノードが存在しない場合、ノードG_jにマーキングする(重みを追加する?)
     7. マーキングしたグラフに対応するスライス、ラベルのみ保持し、watershedのseedにする
        1. (slice, label, sum)で真理値とったらいい?
     8. original, seed 2つでwatershed走らせる
# 2026/06/09
- 下顎は自動化できていそう
- 上顎の自動化と、上顎・下顎の分け方を検討する
# 2026/06/17
- 方針
  - 前歯と臼歯を分割してwatershedしてみる
    - 前歯と臼歯の分け方の検討
      - theshold?
      - archを検出して、前方向を特定。そこから大まかな割合?
  - 疑似Capture.tifを作ってその画像とaxial面を対応させる
# 2026/06/23
- 前歯と臼歯の分割
  - PCA
- 分割で生じる seed のはみだし
  - Seed 画像を楕円でフィッティングする。
    - -> はみだし部分が悪い方向に影響している。
    - フィッティングの方法を模索&重み付きの方法検討
- 上顎と下顎の分割
  - 未定
- 咬合面付近のスライスの判定
  - 未定
# 2026/07/09
- 歯の抽出が微妙だと、Seedが決まらないため無理そう
- 根管をベースにSeed作成した場合、隣接を気にしないのでうまくいきそう?
  - 距離が大きいところは隣接領域なので、そのあたりまでSeedを拡張させる必要はある
# 2026/08/12
- 自動化仕様メモ
  - 準備
    - 歯の抽出画像 (二値化されていない 3 次元データ)
  - 処理
    - 何らかの二値化処理で根幹部分を抽出する
      - エナメル質/根管 での輝度差を利用する
      - ノイズによる孤立点を消去する
        - 大きく erosion してもよい?
    - 根管領域で領域拡張を行う
      - 根管の大まかな 3D データが抽出できれば良い
    - これまでのグラフに根管のラベルを付与する
      - 隣接判定を閾値ではなく、根管のラベルの相違で決定する
# 2026/08/18
- 普通に根管を Seed にしたらいいんじゃない?
  - -> 40x40 のデータでうまくいった。 (n=1)
# 2026/08/20
- 40x40 ではうまくいっている
  - 課題: 根管を抽出するための閾値の決め方
    - ヒストグラムでは、 平坦-山-小山 という感じで分布していて、根管は平坦部に対応しているから、そこを抜き取ったらいいのか?
    - 本当は歯のプロファイルをとって二値化したい
  - 半分しか映っていないなどの見切れた部分は、根管が背景に隣接しているなら、歯冠部の隣接までラベルを拡張する方針で対応した。
  - 根管が映っていないけど歯冠部だけある、みたいなデータはどうしようもない。困った。
    - 学習させるときに切り取ればいいから問題ないらしい。
# 2026/08/25
- やったこと
  - 根管の編集をするときに、 Axial 方向で見切れている歯も削除してしまう問題
    - 隣接 voxel は削除対象にしないように分岐
    - -> 根管が浅く検出できるところに弱い
  - 根管の情報を属性に付与してグラフ作成
    - 隣接スライスは根管がいないことが多い。どう属性を加えるか検討する必要あり
- やること
  - いい具合に根管の属性を保ちながら、臼歯部の隣接と歯の隣接条件を見極める。

## 根管を Seed に使う場合
- 前提条件
  - 根管がつながっていること

### 仕様
- 最終目標
  - Watershed を自動化して自動アノテーションをする
- input 画像
  - 歯のみ抽出された画像
- seed画像
  - ラベリングされた画像
    - 同一の歯は同一のラベリングがされていること
  - seed の準備
    - 1. ImageJ で1データ1歯でのアノテーションをする (黒)
    - 2. 二値化して反転する(背景+アノテーション領域を255にする)
    - 3. 連結成分を抽出する
    - 4. すべての成分を同じ値にする (1データ1歯のため)

- Seed 作成仕様 (2026/07/06)
  - 上顎・下顎をスライス番号で分割 (手入力)
  - 上顎・下顎でそれぞれ PCA を行い、前歯部・臼歯部の分割線を引く
  - (上顎・下顎, 前歯・臼歯)で Graph を用いた Seed 候補を選定する
    - Connected Component ごとに前後スライスの包含関係を見る
    - 統合する領域が閾値以上かつそれ以前のスライスで Seed 候補がいない場合、 Seed 候補とする
  - 50% くらい縮小して、楕円フィッティングする
- Seed 作成仕様 (copilot)
  - 対象コード
    - Watershed/src/graph_rev4/pipeline.py

  - ルートA: run_pipeline_for_jaw_ranges (上顎/下顎を明示して実行)
    - 1. 入力ボリュームを読み込む
      - load_volume(config.input_path)
    - 2. lower_jaw, upper_jaw のスライス範囲ごとに処理する
      - jaw_volume = original_volume[start:end]
    - 3. Seed 作成 (edit前)
      - _build_seed_for_jaw_region を実行
      - 内部で前歯/臼歯へPCA分割し、各領域で graph_seed_volume_bidirectional を実行
      - 生成した seed をラベル振り直しして結合
      - 出力: *_seed_before_edit.tif
    - 4. Seed 編集 (edit後)
      - seed>0 を対象に、各スライスで距離変換上位 keep_ratio の領域のみ残す
      - _trim_volume_by_top_far_region(..., keep_ratio=0.5)
      - その後 _relabel_components_per_slice で再ラベリング
      - 出力: *_seed_after_edit.tif
    - 5. 顎ごとに Watershed を実行
      - watershed_3d_volume(volume=jaw_volume, seed_volume=seed_after_edit)
      - 出力: *_watershed.tif
    - 6. 上下顎 seed を全体座標で結合し、全体 Watershed も実行
      - combined_jaw_seed_after_edit.tif
      - combined_jaw_watershed.tif

  - ルートB: graph_main_anterior_molar_pca_rev4 (単一ボリューム実行)
    - 1. 入力読み込み
    - 2. （必要時）咬合平面アライメント
      - align_occlusal_plane
    - 3. 実行スライス範囲マスク適用
      - create_execution_slice_mask / apply_execution_slice_mask
    - 4. PCAで前歯/臼歯に分割
      - split_volume_anterior_molar_by_pca
      - 出力: anterior_molar_pca_split_labels.tif
    - 5. 各領域でグラフseed生成
      - graph_seed_volume_bidirectional
      - remove_edge で枝刈り
      - detect_non_bifurcating_components + add_largest_area_seed_in_components で seed 補完
      - create_seed_for_watershed
    - 6. 前歯/臼歯 seed を再ラベルして統合
      - 出力: combined_anterior_molar_seed_volume.tif
    - 7. Watershed 実行
      - watershed_3d_volume(volume, combined_seed)
      - 出力: watershed_anterior_molar_rev4.tif
    - 8. 実行情報をJSON保存
      - rev4_pca_graph_watershed_info.json

  - 備考
    - direction_mode は forward_only / reverse_only / bidirectional を選択可能
    - run_pipeline_for_jaw_ranges では upper_jaw の seed 作成時のみ reverse_only を強制
    - run_pipeline_for_jaw_ranges の trim_keep_ratio=0.5 が現在の seed 編集パラメータ